@using Facepunch.Gunfight.UI
@using Sandbox.Menu
@using Sandbox.UI;

@inherits RootPanel
@implements Sandbox.Menu.IGameMenuPanel
@namespace Facepunch.Gunfight.MainMenu

<root>
	<MainMenu />
</root>

@code
{
    protected override void UpdateScale( Rect screenSize )
    {
        base.UpdateScale( screenSize );

        Scale *= UI.UIGlobals.MenuScale;

        // if ( Game.IsRunningOnHandheld )
        // {
        //     Scale += 0.3f;
        // }
    }
}
